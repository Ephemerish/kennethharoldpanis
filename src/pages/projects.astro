---
import Layout from "../layouts/MainLayout.astro";
import Card from "../components/Card.astro";
import FeaturedProject from "../components/FeaturedProject.astro";
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";

// Get all projects and sort by status and date
const allProjects: CollectionEntry<"projects">[] = (
  await getCollection("projects")
).sort((a, b) => {
  // Featured projects first
  if (a.data.featured && !b.data.featured) return -1;
  if (!a.data.featured && b.data.featured) return 1;
  
  // Then by status priority (completed, in-progress, planned)
  const statusPriority = { completed: 0, "in-progress": 1, planned: 2 };
  const statusDiff = statusPriority[a.data.status] - statusPriority[b.data.status];
  if (statusDiff !== 0) return statusDiff;
  
  // Finally by start date (newest first)
  return b.data.startDate.valueOf() - a.data.startDate.valueOf();
});

// Get featured projects
const featuredProjects = allProjects.filter(project => project.data.featured);
const regularProjects = allProjects.filter(project => !project.data.featured);

const pageTitle = "Projects";
---

<Layout 
  title="Projects - Kenneth Harold Panis"
  description="Explore my portfolio of projects including IoT systems, web applications, mobile apps, and hardware prototypes. From LoRa communication networks to modern web development."
  image="/me.jpg"
  type="website"
>
  <div class="min-h-screen bg-hero-gradient">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-16">
      <!-- Header Section with Enhanced Design -->
      <div class="text-center mb-16 sm:mb-20 relative">
        <!-- Decorative background -->
        <div class="absolute inset-0 -z-10">
          <div class="absolute top-0 left-1/2 w-96 h-96 bg-brand-100/30 rounded-full blur-3xl transform -translate-x-1/2 -translate-y-1/2"></div>
        </div>
        
        <div class="animate-fade-in">
          <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold mb-6">
            <span class="text-neutral-900">My</span> <span class="text-brand-gradient">Projects</span>
          </h1>
          <p class="text-xl sm:text-2xl text-neutral-700 max-w-4xl mx-auto leading-relaxed px-2 mb-8">
            A collection of things I've built, experiments I've tried, and problems I've solved.
          </p>
          <div class="w-32 h-1 bg-brand-gradient mx-auto rounded-full"></div>
        </div>
      </div>

      <!-- Featured Projects with Simplified Styling -->
      {featuredProjects.length > 0 && (
        <section class="mb-16 sm:mb-20">
          <div class="space-y-12 sm:space-y-16">
            {featuredProjects.map((project) => (
              <div class="bg-card-gradient rounded-3xl p-8 sm:p-12 shadow-floating hover-lift border border-neutral-200/50 animate-fade-in">
                <FeaturedProject project={project} />
              </div>
            ))}
          </div>
        </section>
      )}

      <!-- Regular Projects Grid with Enhanced Styling -->
      {regularProjects.length > 0 && (
        <section class="mb-16 sm:mb-20">
          <div class="text-center mb-12 sm:mb-16">
            <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-neutral-900 mb-4">
              {featuredProjects.length > 0 ? (
                <>🚀 <span class="text-brand-gradient">Other</span> Projects</>
              ) : (
                <>🚀 <span class="text-brand-gradient">All</span> Projects</>
              )}
            </h2>
            <p class="text-lg text-neutral-600 max-w-2xl mx-auto">
              Exploring different technologies and solving various challenges
            </p>
            <div class="w-20 h-1 bg-brand-gradient mx-auto mt-4 rounded-full"></div>
          </div>
          
          <div class="grid grid-cols-1 gap-8 sm:gap-10 sm:grid-cols-2 lg:grid-cols-3">
            {regularProjects.map((project, index) => (
              <div class="hover-lift animate-fade-in" style={`animation-delay: ${index * 0.1}s`}>
                <Card item={project} type="project" />
              </div>
            ))}
          </div>
        </section>
      )}

      <!-- Call to Action Section -->
      <section class="text-center py-16 sm:py-20">
        <div class="max-w-4xl mx-auto">
          <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-neutral-900 mb-6">
            Like what you see? <span class="text-brand-gradient">Let's collaborate!</span>
          </h2>
          <p class="text-lg sm:text-xl text-neutral-600 mb-8 max-w-2xl mx-auto leading-relaxed">
            I'm always interested in working on exciting projects and solving challenging problems.
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a 
              href="/contact" 
              class="inline-flex items-center px-8 py-4 bg-brand-gradient text-neutral-0 text-lg font-semibold rounded-xl shadow-brand hover:shadow-brand-lg hover-lift transition-all duration-300"
            >
              Get In Touch
              <svg class="ml-3 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
              </svg>
            </a>
            <a 
              href="https://github.com/Ephemerish" 
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center px-8 py-4 bg-neutral-0 text-brand-600 text-lg font-semibold rounded-xl border-2 border-brand-500 hover:bg-brand-gradient hover:text-neutral-0 hover:shadow-brand-lg hover-lift transition-all duration-300"
            >
              View GitHub
              <svg class="ml-3 w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
              </svg>
            </a>
          </div>
        </div>
      </section>
    </div>
  </div>
</Layout>
